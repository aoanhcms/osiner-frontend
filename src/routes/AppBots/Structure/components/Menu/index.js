import React from 'react';

export const Block = ({name}) => {
  return (<div className="gx-structure-menu-block">
    {name}
  </div>)
}
export const Group = ({name}) => {
  return (<div className="gx-struture-menu-group">
    {name}
  </div>)
}
